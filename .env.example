PORT=5000
NODE_ENV=development

# Optional for future AI-backed scoring/recommendations.
# The app runs offline with local framework scoring when no API key is set.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
